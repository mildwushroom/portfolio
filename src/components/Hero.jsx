import { motion } from 'framer-motion';
// import the canvas for whatever 3d thing you're bringing in 

const Hero = () => {

  return (
    <section>
      Hero
    </section>
  )
};

export default Hero;